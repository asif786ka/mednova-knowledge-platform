# --- core (required to run the prototype) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
numpy>=1.26
networkx>=3.1
python-dotenv>=1.0

# --- document loaders (optional formats) ---
pypdf>=4.0            # .pdf ingestion
python-docx>=1.1      # .docx ingestion

# --- optional: better embeddings (falls back to hashing embedder if absent) ---
# sentence-transformers>=2.6

# --- LLM provider abstraction (extractive fallback used if no key set) ---
litellm>=1.35

# --- MCP: model context protocol client + local server (agnostic transport) ---
mcp>=1.2

# --- optional: caching / queue broker ---
redis>=5.0

# --- optional: observability ---
# langfuse>=2.0

# --- optional: graph visualisation (interactive HTML) ---
pyvis>=0.3
graphviz>=0.20        # static PNG (needs system `graphviz`/`dot`)

# --- dev / test ---
pytest>=8.0
httpx>=0.27
